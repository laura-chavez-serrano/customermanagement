
 
<%= form_with(model: @reminder, local: true) do |form| %>
 <% if @reminder.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@reminder.errors.count, "error") %> prohibited
        this reminder from being saved:
      </h2>
      <ul>
        <% @reminder.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% if notice %>
  <p><%= notice %></p>
<% end %>
 
  <p>
    <%= form.label :close_type %><br>
    <%= form.text_field :close_type %>
  </p>
 
  <p>
    <%= form.label :periods_remind %><br>
    <%= form.text_area :periods_remind %>
  </p>
 
  <p>
    <%= form.submit %>
  </p>
 
<% end %>
 
<%= link_to 'Back', reminders_path %>

